# Architecture Decision Records

Short records for decisions that are easy to accidentally undo.

Use these when a decision affects behavior across sessions, debugging traces, or regression tests. Keep each ADR focused on one decision and reference the code/test that protects it.

When code has a `SEE-ADR-###` comment, read the referenced ADR before changing that path.
